# Normalize line endings to LF in the repo across platforms.
* text=auto eol=lf

# Treat lockfiles and generated assets as binary-ish (no eol munging, no diff noise).
package-lock.json -diff
*.png binary
*.ico binary
